# auth

Autenticação da perita e da secretária (2 perfis, sem sistema de papéis complexo).
Login via OTP (Resend/SMTP) — ver stack técnico no CLAUDE.md da raiz.

Vai conter: lógica de envio/verificação de código OTP, sessão, guarda de rotas do
`(dashboard)`.
